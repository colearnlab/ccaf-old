define(function() {
  var exports = {};
  var cb;

  var calculators;
  exports.startApp = function(_cb, parentElement) {
    m.mount(parentElement, Main);
  };

  var Main = {
    'view': function(ctrl, args) {
      return m('div#test', ['Hello!']);
    }
  };

  return exports;
});
